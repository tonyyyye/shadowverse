use v6.c;
use Log::Async;
use lib <lib>;
use Entity;
use Enum;


=para
Shadowverse::Game::Game_jobs::
What a Game can do.

role Game_jobs {
}

=para
Shadowverse::Game::
A Game object is all a user needs.

class Game does Game_jobs {

}
